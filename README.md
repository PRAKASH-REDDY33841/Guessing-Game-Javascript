# Guessing Game Using JavaScript Fundamentals 🎮

This is a simple Number Guessing Game built using **HTML, CSS, and JavaScript**.  
The game generates a random number between **1 and 100**, and the user tries to guess it correctly.

## 🚀 Features
- Random number generation (1–100)
- User input validation
- Shows result messages (Too High / Too Low / Correct)
- Dynamic background color changes based on result
- Responsive design for mobile and tablet screens

## 🛠️ Technologies Used
- HTML5
- CSS3
- JavaScript

## 📚 What I Learned
- DOM manipulation using `getElementById()`
- Using `Math.random()` and `Math.ceil()` for random numbers
- Applying conditional logic using `if-else`
- Handling user input using `parseInt()`
- Updating UI dynamically with JavaScript
- Making the page responsive using CSS media queries

## 📌 How to Run
1. Download or clone the repository
2. Open `index.html` in your browser
3. Start guessing the number 🎯
